<%= form_for(current_user.relationships.find_by(follower_id: user.id), html: { method: :delete }) do |f| %>
  <%= hidden_field_tag :follower_id, user.id %>
  <%= link_to 'フォローを外す', relationships_path(follower: user), remote: true, method: :delete %>
<% end %>