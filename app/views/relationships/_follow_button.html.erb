<% unless current_user == @user %>
  <% if current_user.following?(@user) %>
    <%= link_to 'フォローを外す', relationship_path(follower: user), method: :delete, class: 'btn btn-danger btn-block' %>
  <% else %>
    <%= link_to 'フォローする', relationships_path(follower: user), class: 'btn btn-primary btn-block', method: :post %>
  <% end %>
<% end %>
